{
  "name": "content-simulator",
  "version": "0.2.0",
  "private": true,
  "description": "Content style learning and generation workbench with an OpenAI backend proxy.",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "check": "node --check app.js && node --check server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "mammoth": "^1.8.0"
  }
}
